# ─── LOCAL DEVELOPMENT ───────────────────────────────────────────────
# When running frontend (npm start) locally with backend (npm run dev) locally:
REACT_APP_API_URL=http://localhost:3001

# ─── VERCEL MONOREPO DEPLOYMENT ──────────────────────────────────────
# When deploying as monorepo to Vercel (both frontend + backend same domain),
# comment out the above line and uncomment this:
# REACT_APP_API_URL=

# ─── SEPARATE FRONTEND/BACKEND DEPLOYMENTS ───────────────────────────
# When frontend and backend are deployed separately:
# REACT_APP_API_URL=https://your-backend-url.vercel.app

GENERATE_SOURCEMAP=false
